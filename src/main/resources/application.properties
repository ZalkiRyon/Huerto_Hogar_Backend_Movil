spring.application.name=huertohogar

# Server Configuration - Escuchar en todas las interfaces para acceso desde emulador
server.address=0.0.0.0
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/hh_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# Swagger UI
springdoc.swagger-ui.path=/swagger-ui.html

# Cloudinary Configuration (External API for image hosting)
# SECURITY: Do NOT commit these values to Git. Use environment variables instead.
# Set these in your IDE Run Configuration or System Environment Variables:
# - CLOUDINARY_CLOUD_NAME=your_cloud_name
# - CLOUDINARY_API_KEY=your_api_key
# - CLOUDINARY_API_SECRET=your_api_secret
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dg7dcbcjn}
cloudinary.api-key=${CLOUDINARY_API_KEY:418523925422357}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:dnx-CX1zmeGrkBbkaqC91U2IqmA}

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB